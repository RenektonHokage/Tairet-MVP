# Checkpoint — 9E.5B3B4B

## Objective

Close the reviewed and pushed durable-email runtime composition and lazy `WorkerMain` wiring without treating repository capability as deployment, activation, authority, or authorization.

## State

`CLOSED`

The product contract and identity below are `VERIFIED_REPOSITORY`. Activation and authority remain `REPORTED`; deployment and the independent remote migration ledger remain `UNKNOWN`.

## Product commit

- Commit: `dc261581eb1bafc8d1147ac1fea736d3ceb75edc`
- Parent: `cd133b9ef65c98cab069ba9945533e1fcfe68744`
- Tree: `07abce2027d8f6139602b710d99d8916bff3cd53`
- Message: `feat(access): wire durable email worker capability`
- Stat: 2 files, 487 insertions, 48 deletions (`+487/-48`)
- Product paths: `functions/api/src/workers/accessFulfillmentWorker.test.ts` and `functions/api/src/workers/accessFulfillmentWorkerMain.ts`
- Product changeset: `65f330660ff88201fa1d57ffec3d8370e33691c21648d442de460852f3b35702`

## Closed contract

- Invalid configuration fails before importing or constructing runtime dependencies.
- Worker OFF and dry-run return before Supabase, the Resend-capable email runtime, provider construction, or signal-handler registration.
- Reconcile-only mode preserves its prior client path and constructs no durable reader, builder, provider, or durable-only secret dependency.
- Active durable mode loads one Supabase client shared by the RPC client and read-only message-data reader.
- The canonical loader is bound to that reader; the canonical builder is bound to the validated sender address and QR base URL.
- One Resend provider instance per active process is placed in the exact worker capability, and `provider.send` is not called during startup.
- Import, reader, builder, provider, and worker startup failures fail closed with sanitized logs; registered signal handlers are cleaned up.
- The former `durable_email_capability_not_implemented` hard gate is removed.
- Worker and durable-email defaults remain OFF, legacy direct email defaults ON, and legacy/durable mutual exclusion remains intact.

## Principal validation

Validation was bound to the exact product changeset and completed before product review, commit, and push:

- Targeted worker suite: 102 of 102 passed.
- Complete API suite: 214 of 214 passed.
- Typecheck: passed.
- Build: passed.
- Temporary reproducible directed lint for the two changed TypeScript files: passed.
- Global `functions/api` lint: blocked before analysis by pre-existing lint infrastructure; this is not a B regression.
- Final semantic review: High 0, Medium 0, Low 0.

The directed lint invocation remains changeset-specific evidence, not permanent repository tooling. This documentation closure runs no API test, typecheck, build, lint, SQL, migration, or provider call.

## Runtime and authority boundaries

- Repository capability: lazy durable-email composition is present in `WorkerMain` (`VERIFIED_REPOSITORY`).
- Deployment: no durable worker or durable-email deployment is established (`UNKNOWN`).
- Remote migrations: application of migrations 046–049 is not inferred; the independent ledger remains `UNKNOWN`.
- Activation: worker OFF and durable email OFF (`REPORTED`).
- Legacy activation and authority: direct email remains ON and authoritative in the reported operating mode (`REPORTED`).
- Authority transfer: none; composition does not grant durable email authority (`REPORTED`).
- Authorization: remote apply, deployment, activation, cutover, and rollout are not authorized (`REPORTED`).

This documentation closure performs no migration application, provider call, deployment, activation, cutover, rollout, or authority transfer.

## Closure and sequencing

This closure changes exactly four canonical documents plus this checkpoint. It changes no product, test, config, service, migration, environment, package, lockfile, invariant, or earlier checkpoint.

No next product slice is declared. Any later product work must be named and authorized explicitly. `B6` remains blocked and not authorized; `B7` remains blocked and not authorized. Neither is started by this closure.
