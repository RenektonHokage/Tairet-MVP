# Checkpoint — 9E.5B3B4P

## Objective

Persist a terminal durable-email failure discovered before provider claim/call, fenced by the current fulfillment lease and replayable without inventing provider activity.

## State

`CLOSED`

## Commit

`f0f04f49d138dc56d368dc6bf338ff4e45932c2f` — `feat(access): add preclaim terminal email fence`

## Closed contract

- Migration 047 adds a SHA-256 provenance marker to `access_order_fulfillments`.
- `record_access_email_preclaim_terminal_failure(...)` accepts only a closed set of terminal data error codes.
- The RPC validates order, paid state, issuance completion, email generation, active lease token/epoch, delivery-attempt state, and current email state.
- A successful record moves email to terminal `manual_review`, clears the lease, and creates no provider delivery attempt.
- A lost-response replay is recognized by the canonical request hash and returns an idempotent terminal result.
- Batch claiming excludes a fulfillment already carrying the terminal provenance marker.

## Introduced invariants

- Pre-claim terminal failure does not represent or imply a provider call.
- Plaintext lease tokens are not stored in the provenance marker.
- Processing, ambiguous, or accepted provider-attempt state prevents false pre-claim terminalization.
- Stale lease or generation mismatch carries no authority.
- The RPC is security-definer with fixed search path and executable only by `service_role`.

## Principal validations

- Exact client schema, allowlist, business errors, transport sanitization, and parameter mapping in `accessFulfillment.test.ts`.
- Embedded migration assertions cover the new column/constraint, closed allowlist, immutable batch-function baseline, function signature/security, grants, RLS, and absence of client policies.
- No migration was applied remotely during this slice closure context.

## Migration/hash

- Migration 047: `infra/sql/migrations/047_access_email_preclaim_terminal_failure.sql`
- SHA-256: `6bebbe803cc5c85094352bbcbc261df5ce26780fb1ae38d956089f48b67f20b4`
- Immutable migration 046 baseline SHA-256 asserted by 047: `6f1b39fc66b53856e19475b20539de1377f20e29fe6ad99a79472c97dba2fe0c`

## Accepted debt

- Migration 047 is reported not applied remotely; independent runtime verification is unknown.
- Worker does not invoke the new terminal pre-claim RPC.
- Durable provider, loader, and message contracts are not composed in `WorkerMain`.
- Worker/durable flags remain OFF; legacy direct email remains authority.

## Operational restrictions

- Remote application requires a controlled maintenance window and separate authority.
- Unapplied remote 047 does not block authorized local, reversible ASK/CODE for the next slice; it blocks only activation/deploy/rollout that depends on it.
- Do not start `9E.5B3B4A`, apply 047, enable durable delivery, or change authority without a new instruction.

## Relation to next slice

`9E.5B3B4A` is next eligible but not authorized. Its plan must compose only the explicitly approved local surface and preserve all deployment and authority gates in [`ROADMAP.md`](../../ROADMAP.md).
