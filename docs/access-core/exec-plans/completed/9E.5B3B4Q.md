# Checkpoint — 9E.5B3B4Q

## Objective

Close the poison loop when a terminal preclaim appears after a current `ambiguous` attempt that can no longer be reclaimed because its canonical payload is unavailable.

## State

`CLOSED`

## Commit

`561bb2daeff9979687149f0442f5e53675c9da70` — `feat(access): handle terminal preclaim after ambiguous`

## Closed contract

- Migration 048 is a function-only replacement of the existing terminal preclaim RPC; its signature and response contract remain unchanged.
- Exactly one historical current-generation `ambiguous` attempt may coexist only when `email_status = 'failed'`; `processing` and `accepted` attempts continue to block terminal recording.
- The preserved `ambiguous` attempt remains byte-for-byte intact as historical provider evidence.
- Successful recording moves fulfillment to `manual_review` and stores the marker for the exact terminal request.
- The operation creates no delivery claim, attempt, provider call, or provider outcome.
- Exact lost-response replay remains idempotent.

## Introduced or clarified invariants

- “Preclaim” means before starting a new delivery claim or provider call; it does not deny historical provider activity.
- The terminal request marker and the preserved attempt are separate evidence with separate meaning.
- Only exactly one historical current-generation `ambiguous` attempt may coexist with terminal recording.
- Terminal recording never fabricates a provider outcome.
- Deterministic lock order preserves a single winning mutation authority in races with reclaim.

## Principal validations

Closure evidence bound to the exact product commit and changeset recorded (`REPORTED`):

- migration apply in isolated local PostgreSQL, structural assertions, and ACL assertions;
- migration 047 regression plus all eight ordinary terminal error codes;
- all eight terminal codes with an open ambiguous attempt and all eight with an expired ambiguous attempt;
- exact replay, full attempt immutability, and unchanged `provider_call_count`;
- reclaim-first and 048-first races, with zero deadlocks and zero provider calls.

## Migration/hash

- Path: [`infra/sql/migrations/048_access_email_preclaim_terminal_after_ambiguous.sql`](../../../../infra/sql/migrations/048_access_email_preclaim_terminal_after_ambiguous.sql)
- Migration 048 SHA-256: `249a75c0da2f80a66e0d0717a88d8c4710cf51f210098078a375692fd4567532`
- Migration 048 normalized body SHA-256: `d725a12249ebad5606994e8a6cb8dc1670dd1211f882b689f7db523b8befabc4`
- Preserved migration 046 SHA-256: `6f1b39fc66b53856e19475b20539de1377f20e29fe6ad99a79472c97dba2fe0c`
- Preserved migration 047 SHA-256: `6bebbe803cc5c85094352bbcbc261df5ce26780fb1ae38d956089f48b67f20b4`

## Changeset identity

`f187d9a38dbc397392270d0a6813170d7c8fb78b2d32dba9189c356636eea4ee`

## Accepted debt

- Migration 048 is reported not applied remotely (`REPORTED`); independent remote migration-ledger state remains `UNKNOWN`.
- The worker does not invoke the durable email state machine, and decisions D1/D2/D3 are not yet materialized in worker behavior (`VERIFIED_REPOSITORY`).
- The `WorkerMain` durable-email hard gate remains intact (`VERIFIED_REPOSITORY`).
- Worker and durable email activation remain OFF, while legacy direct email retains authority (`REPORTED`).

## Operational restrictions

- Do not apply migration 048 remotely without separate authorization.
- Do not activate durable email or transfer authority.
- Do not start `B6` cutover or `B7` rollout.
- Do not infer production state from repository presence or the pushed commit.

## Relation to next slice

`9E.5B3B4A` is the next eligible slice, but it is not authorized and has not started. A new instruction and focused ASK/delta discovery are required before implementation.
