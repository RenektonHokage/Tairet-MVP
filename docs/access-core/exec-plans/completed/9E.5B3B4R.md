# Checkpoint — 9E.5B3B4R

## Objective

Close the contract mismatch before `9E.5B3B4A` by adding authoritative delivery-claim correlation and a compatible parser without composing or activating the worker.

## State

`CLOSED`

## Product commit

- Commit: `775de00486a6032e7ec94558ec0d0d6492647699`
- Parent: `9d01b8acaf62a9f9116bc90d5625ccc56fc8456a`
- Tree: `8d720e7cc59636e11c3c4fed85a0749da374db65`
- Message: `feat(access): add email claim correlation`
- Stat: 5 files, 2376 insertions, 36 deletions

## Closed contract

- Migration 049 is additive to the public `processing` response, transactional, and function-only. It creates no table, column, constraint, index, or backfill.
- `claim_access_email_delivery` retains its seven arguments, argument order and types, the `resend` provider default, and its `jsonb` result.
- Owner `postgres`, `SECURITY DEFINER`, `search_path = public, pg_temp`, function OID, and the closed EXECUTE ACL are preserved.
- Baseline branches, locks, fencing, mutations, provider-call accounting, idempotency key, persisted expiry, errors, and `skipped_sent` remain unchanged.
- Fresh claim, processing replay, and ambiguous reclaim expose one homogeneous correlated `processing` shape.
- That shape adds exactly `entry_count`, `request_payload_hash`, and `idempotency_remaining_ms`.
- Count and both hashes come from final `v_attempt` state rather than untrusted response inputs.
- Each branch samples `clock_timestamp()` after its final mutation and returns the floored, nonnegative duration from persisted expiry. SQL neither extends expiry nor adds a provider margin.
- Public responses expose neither `idempotency_expires_at` nor `database_now`, version, claim mode, or provider-call count.
- PostgreSQL is the authority for remaining duration. The future Node worker discounts it only by monotonic elapsed time from the conclusive claim attempt.
- The fixed 1000 ms stage margin covers local jitter, quantization, and synchronous provider handoff; it is not clock-skew allowance.
- The parser accepts complete strict legacy `processing` and strict correlated `processing`.
- Correlated fields must be complete; extras, invalid count or UUID entries, non-lowercase SHA-256, mismatched key, or invalid remaining duration fail.
- Partial correlated responses are malformed rather than downgraded to legacy evidence.
- A null, omitted, or undefined error `delivery_attempt_id` normalizes to absence; a valid UUID remains correlated and invalid non-null data is malformed.
- The product commit changes no worker or `WorkerMain` file, and the durable-email hard gate remains intact.

## Validation

Validation is bound to the exact product commit and product changeset below.

- Isolated PostgreSQL/Supabase 17.6 containers used `network=none` and applied migrations 046 through 049 in order.
- The 48-case SQL matrix covered fresh, replay, reclaim, lost response, near expiry, null/UUID drift, and baseline regressions.
- Two races ran across four sessions with zero deadlocks and one winning mutation authority per race.
- Targeted claim-parser tests passed 29/29; the complete API suite passed 158/158.
- Typecheck and build passed. Directed lint of the two changed TypeScript files passed with zero errors and warnings.
- Global API lint was blocked before analysis by pre-existing lint infrastructure, not by R.
- Diff, byte identity, UTF-8/LF, relative links, local paths, secrets, private-email, and PII checks passed.

PostgreSQL evidence is `VERIFIED_RUNTIME` for an isolated local runtime only. Source, test, commit, and changeset evidence is `VERIFIED_REPOSITORY`. Neither is production, remote-ledger, or deployment evidence.

## Identity

- Migration 046: `6f1b39fc66b53856e19475b20539de1377f20e29fe6ad99a79472c97dba2fe0c`
- Migration 047: `6bebbe803cc5c85094352bbcbc261df5ce26780fb1ae38d956089f48b67f20b4`
- Migration 048: `249a75c0da2f80a66e0d0717a88d8c4710cf51f210098078a375692fd4567532`
- Migration 049: `cc3b71a988fafa3298344ec73731abc0ce8219c1dc35110e59916e5344f6d234`
- Normalized final claim body: `bdfba3af34357decbc07794ee09c4cb85d3b62f1d2760a1996e1b81023aab324`
- Normalized baseline after only authorized additions are removed: `0eed47f60a2fe2c85052e502936cfbffef5e0888f15a1c594636379daa26a78f`
- Product changeset: `3c9d13cc18ce448749d080be2edd58b2f18142452071c18d5c249cea80e86c43`

## Runtime and authority

- Migrations 047, 048, and 049 were reported not applied remotely during their closures (`REPORTED`).
- Current independent remote migration-ledger state is `UNKNOWN`.
- The closure made no production access, provider call, remote migration application, deployment, activation, or authority transfer (`REPORTED`).
- Worker and durable email remain OFF; legacy direct email retains authority (`REPORTED`).
- Durable email has no operational authority, and R transfers none (`REPORTED`).
- The `WorkerMain` durable-email hard gate remains intact (`VERIFIED_REPOSITORY`).
- Repository presence and the pushed product commit do not establish deployment or production state.

## Accepted limitation

Independent persistence and comparison of `retryAfterSeconds` during outcome replay remains future hardening, not a blocker for R. The future worker freezes and replays the exact outcome tuple.

## Relation to B3B4A

- `9E.5B3B4A` is next eligible, not authorized, and not started (`REPORTED`).
- A brief delta ASK must reconfirm the expected two-file scope: `functions/api/src/workers/accessFulfillmentWorker.ts` and `functions/api/src/workers/accessFulfillmentWorker.test.ts`.
- That scope remains subject to discovery and separate human authorization before CODE.
- `WorkerMain`, `9E.5B3B4B`, remote migration application, deployment, and activation are outside B3B4A and this closure.
- R closes only the correlation prerequisite; it does not implement the worker state machine.
