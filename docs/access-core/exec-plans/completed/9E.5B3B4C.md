# Checkpoint — 9E.5B3B4C

## Objective

Close the reviewed and pushed HTTP legacy direct-email authority gate without treating repository capability as deployment, activation, authority transfer, cutover, or rollout.

## State

`CLOSED`

The product contract and identity below are `VERIFIED_REPOSITORY`. Activation and operational authority remain `REPORTED`; deployment and the independent remote migration ledger remain `UNKNOWN`.

## Product commit

- Commit: `28690a936ddf9f7388066bde03aceeae7150408a`
- Parent: `37a6e77b88dccdfdd9197bc9cf0f770b38f6525a`
- Message: `fix(access): enforce legacy email authority gate`
- Product changeset: `6f57277487dde74c975ce4a867a3146738d8b04545e0764ddefdb213764a368f`
- Product paths: `functions/api/package.json`, `functions/api/src/config/accessFulfillment.test.ts`, `functions/api/src/config/accessFulfillment.ts`, `functions/api/src/services/bancardConfirm.test.ts`, and `functions/api/src/services/bancardConfirm.ts`

## Closed contract

- `ACCESS_LEGACY_DIRECT_EMAIL_ENABLED` has a strict narrow reader: omitted defaults to `true`, explicit `"true"` and `"false"` are accepted, and every other value is invalid.
- After private-key and timing-safe token validation, the Bancard callback reads and captures the gate once per invocation before the first mutating RPC. Invalid configuration fails closed before payment mutation.
- Gate `false` produces legacy loader calls `0` and sender calls `0`; default or explicit `true` preserves legacy delivery.
- The legacy sender is loaded lazily only after approved issuance, and dependencies are invocation-local without a mutable module-global override.
- Rejected and manual-review results, confirmation failures, and issuance failures do not load the sender.
- Payment confirmation, entry issuance, and the HTTP response contract are preserved. Email failures after issuance remain non-blocking.

## Principal validation

Validation was bound to the exact product changeset and completed before product review, commit, and push:

- Configuration suite: 14 of 14 passed.
- Bancard callback suite: 17 of 17 passed.
- Complete API suite: 235 of 235 passed.
- Typecheck: passed.
- Build: passed.
- Temporary reproducible directed lint for the changed TypeScript surface: passed.
- Global `functions/api` lint: blocked before analysis by pre-existing lint infrastructure; this is not a C regression.
- Final semantic review: High 0, Medium 0, Low 0.

The directed lint invocation remains changeset-specific evidence, not permanent repository tooling. This documentation closure runs no API test, typecheck, build, lint, SQL, migration, or provider call.

## Runtime and authority boundaries

- Repository default: legacy direct email remains `true` (`VERIFIED_REPOSITORY`).
- Reported activation: worker OFF, durable email OFF, and legacy direct email ON (`REPORTED`).
- Reported authority: the legacy direct-email path remains authoritative; this slice transfers no authority (`REPORTED`).
- Deployment and remote migrations: no deployment is established, and application of migrations 046–049 is not inferred; both remain `UNKNOWN`.
- Disabling the legacy sender by configuration does not by itself activate durable email or transfer authority.

This product slice and documentation closure perform no migration application, real provider call, deployment, activation, cutover, rollout, or authority transfer.

## Closure and sequencing

This closure changes exactly five canonical documents plus this checkpoint. It changes no product, test, configuration, service, callback, migration, environment, package, lockfile, or earlier checkpoint.

No next product slice is declared: `NONE_DECLARED`. Any later product work must be named and authorized explicitly. `B6` remains blocked and not authorized; `B7` remains blocked and not authorized. Neither is started by this closure.
