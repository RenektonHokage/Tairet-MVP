# Checkpoint — 9E.5B3B4A

## Objective

Close the reviewed and pushed local durable-email worker state-machine core without treating repository capability as deployment, activation, authority, or authorization.

## State

`CLOSED`

The product contract and identity below are `VERIFIED_REPOSITORY`. Reported activation and authority remain `REPORTED`. The current independent remote migration ledger remains `UNKNOWN`.

## Product commit

- Commit: `3ba88e82bd2686ba41c545712cebfd7f326a3c43`
- Parent: `ec80d1a2dde89ffe6d88cbfba4728273358262ce`
- Tree: `266679d3b613968f6f87f1f6248adbce7abed74b`
- Message: `feat(access): add durable email worker state machine`
- Stat: 2 files, 6,225 insertions, 100 deletions (`+6225/-100`)
- Product paths: `functions/api/src/workers/accessFulfillmentWorker.ts` and `functions/api/src/workers/accessFulfillmentWorker.test.ts`

## Closed contract

- The worker captures and validates an injectable durable-email capability; durable-email OFF preserves issuance/reconciliation and defers delivery.
- Durable-email ON uses a phased machine with the canonical loader/builder, terminal preclaim, migration-049 correlated claim, provider handoff, and settlement.
- Only complete correlated `processing` grants provider authority. SQL remaining duration is discounted by monotonic elapsed time, and the 1,000 ms stage margin is not clock-skew allowance.
- Insufficient margin starts no provider, release, or new claim and settles the closed retryable outcome.
- Legacy `processing` grants no provider authority; it is preserved as ambiguous, settled when possible, and followed by a fatal stop.
- The provider starts at most once; provider text is not classification authority; the selected outcome tuple is immutable after start.
- Seven independent email counters preserve accounting without redefining issuance/reconciliation results.
- Logs exclude buyer PII, access or lease tokens, idempotency keys, provider keys, and provider identifiers.
- `accessFulfillmentWorkerMain.ts` is byte-identical to the parent, and its durable-email hard gate remains intact.

## State machine and authority

Pre-provider transitions depend on generation, lease token, epoch, and conclusive SQL responses; local control flow never creates database mutation authority. Terminal preclaim and delivery claim each have at most two exact attempts.

Only correlated `processing` crosses the provider boundary. Afterward, release and another claim are prohibited, including after insufficient margin, ambiguity, settlement exhaustion, shutdown, or fatal cancellation.

The capability remains local repository capability. A does not compose startup, remove the `WorkerMain` gate, enable a flag, deploy, or transfer authority.

## Provider and settlement semantics

The final timing and authority guard is synchronous immediately before `provider.send`; one obligation permits at most one provider start.

The provider signal observes timeout, shutdown, and first-fatal cancellation. Abort or uncertainty after start is ambiguous, never a safe retry conclusion.

The allowlisted outcome tuple is frozen across settlement. Post-provider settlement has at most two exact attempts and an independent bounded signal so global cancellation cannot drop started obligations; exhaustion is unsettled and fatal, with no new authority.

## Concurrency and accounting

The first fatal condition wins and cancels new work without overwriting itself or discarding provider and settlement obligations already started.

Durable email exposes these seven orthogonal counters:

- `emailAcceptedCount`
- `emailRetryScheduledCount`
- `emailAmbiguousCount`
- `emailSkippedSentCount`
- `emailUnsettledCount`
- `emailManualReviewCount`
- `emailManualReviewUnknownCount`

The counters are orthogonal: none implies another or turns issuance/reconciliation completion into email acceptance.

## Principal validation

Validation was bound to the exact product changeset and completed before the product commit and push:

- Targeted worker suite: 100 of 100 passed.
- Complete API suite: 212 of 212 passed.
- Typecheck: passed.
- Build: passed.
- Temporary reproducible directed lint for the two changed TypeScript files: passed.
- Global `functions/api` lint: blocked before analysis by pre-existing lint infrastructure; this was not classified as an A regression.
- Final semantic review: High 0, Medium 0, Low 0.

The global lint limitation does not turn the temporary directed invocation into permanent repository tooling. This documentation closure reruns no API test, typecheck, build, lint, SQL container, migration, or provider call.

## Changeset identity

- Product changeset: `58cc5ad4b8cfad7b9d90e920b49a3692374c5472bdd6492deb3ab507f8eef02c`
- Worker SHA-256: `f75fad0334ebbd8283936c2fe7efbb5205afb334cc9fb2554b10df269f750c2f`
- Worker test SHA-256: `7234e5779c22da7c4e0f2329e79849c59c0b60d37e00052fc5ffe7525979edf1`
- Migration 046 SHA-256: `6f1b39fc66b53856e19475b20539de1377f20e29fe6ad99a79472c97dba2fe0c`
- Migration 047 SHA-256: `6bebbe803cc5c85094352bbcbc261df5ce26780fb1ae38d956089f48b67f20b4`
- Migration 048 SHA-256: `249a75c0da2f80a66e0d0717a88d8c4710cf51f210098078a375692fd4567532`
- Migration 049 SHA-256: `cc3b71a988fafa3298344ec73731abc0ce8219c1dc35110e59916e5344f6d234`
- `WorkerMain` SHA-256: `046924c715e2a892fdc126eebcd40dbbc26ef4ab6ef5546c9435ca2b722d081e`
- `WorkerMain` Git blob in parent, product commit, and worktree: `b556cc6aa9cf411ecd6a763d95fe609c05ed2323`

The product commit is accessible locally, matches its parent, tree, subject, and stat, and was equal to the local `origin/main` tracking ref at documentation preflight.

## Preserved surfaces

The product commit changes only the worker and its test; `WorkerMain`, configuration, services, migrations, manifests, lockfiles, environment, callbacks, providers, legacy sender, and production files are preserved.

This closure changes five canonical documents plus this checkpoint and creates no active plan, script, manifest, migration, fixture, workflow, or product edit. Protected untracked evidence and the stash remain preserved.

## Runtime and authority

- Repository capability: the local durable-email worker core is present (`VERIFIED_REPOSITORY`).
- Deployment: no durable worker or durable-email deployment is established by this closure (`UNKNOWN`).
- Remote migrations: application of migrations 046–049 is not inferred; the current independent remote ledger is `UNKNOWN`.
- Activation: worker OFF and durable email OFF (`REPORTED`).
- Legacy activation: direct email ON in the reported operating mode (`REPORTED`).
- Composition: the durable capability is not composed in `WorkerMain`, whose hard gate remains intact (`VERIFIED_REPOSITORY`).
- Authority: legacy direct email retains authority; durable email has none (`REPORTED`).
- Authorization: remote apply, deployment, activation, cutover, and rollout are not authorized (`REPORTED`).

A closes local repository capability only. It performs no production access, remote migration application, deployment, activation, cutover, rollout, provider call, or authority transfer.

## Accepted debt

- The `WorkerMain` capability remains uncomposed and the hard gate remains intact.
- Durable email and the worker remain OFF; legacy direct email retains authority.
- The independent remote migration-ledger state remains `UNKNOWN`.
- A permanent working API ESLint configuration is still missing; directed lint remains temporary review evidence.
- Deployment, activation, cutover, and rollout were not performed or authorized.

These restrictions are boundary conditions for later work, not evidence that A is incomplete as a local worker-core slice.

## Relation to B3B4B

`9E.5B3B4B` is next eligible, not authorized, and not started (`REPORTED`). Its bounded purpose is runtime composition and `WorkerMain` startup wiring and requires separate scope and authorization.

B3B4B grants no remote apply, deployment, activation, legacy disablement, or authority transfer. Composition alone transfers no authority. Remote migration verification remains a deployment/activation gate; B6 and B7 remain blocked and unchanged.
