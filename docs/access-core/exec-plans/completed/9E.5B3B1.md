# Checkpoint — 9E.5B3B1

## Objective

Define deterministic Access Core email messages, canonical provider payloads, and reproducible request hashes while preserving visible legacy email behavior.

## State

`CLOSED`

## Commit

`d59d2dfd138053bdd6ab7f9c87d4cc790ca947e4` — `feat(access): add deterministic email payloads`

## Closed contract

- Template version is explicit and part of the durable request identity.
- Canonical entry order follows stable PostgreSQL-compatible UUID ordering rather than received row order.
- Provider-visible fields and attachments are built deterministically.
- The request hash covers the canonical provider payload and template version, not the delivery idempotency key.
- Existing visible legacy rendering remains preserved while durable output is canonicalized.

## Introduced invariants

- The same logical input produces the same message and hash.
- Duplicate/invalid entry identities, units, tokens, addresses, and invalid durable dates fail closed.
- Hashing does not mutate the message.
- Returned message structures are defensive and frozen.
- QR content remains opaque-token based and does not expose internal IDs.

## Principal validations

- Canonical message, ordering, hashing, immutability, date-boundary, and invalid-input cases in `accessEmailMessage.test.ts`.
- QR payload and validation cases in `accessQr.test.ts`.
- Compatibility coverage in the existing Access Core direct-email service tests.
- Full command logs are intentionally excluded.

## Migration/hash

No SQL or migration change.

## Accepted debt

- Canonical repository data loading was not yet implemented.
- No durable provider adapter or worker orchestration.
- No activation or authority change.

## Operational restrictions

- Do not infer provider delivery from successful rendering.
- Do not use canonicalization to switch away from legacy authority.
- Do not log recipient PII or full entry/check-in tokens.

## Relation to next slice

`9E.5B3B2` supplies validated canonical repository data to this deterministic message contract.
