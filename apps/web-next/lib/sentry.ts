// TODO: Configurar Sentry
// import * as Sentry from "@sentry/nextjs";

export function initSentry() {
  // TODO: Inicializar Sentry con DSN desde env
  if (typeof window !== "undefined") {
    // Sentry.init({ ... });
  }
}

